import React from "react";
import { Typography } from "antd";

const { Title } = Typography;

const SelectionResult = () => {
  return (
    <div>
      <Title level={2}>选题结果</Title>
    </div>
  );
};

export default SelectionResult;
