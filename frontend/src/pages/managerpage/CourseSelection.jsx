import React from "react";
import { Typography } from "antd";

const { Title } = Typography;
const CourseSelection = () => {
  return (
    <div>
      <Title level={2}>选课操作</Title>
    </div>
  );
};

export default CourseSelection;
