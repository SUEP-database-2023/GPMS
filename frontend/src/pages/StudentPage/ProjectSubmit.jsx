import React from "react";
import { Typography } from "antd";

const { Title } = Typography;
const ProjectSubmit = () => {
  return (
    <div>
      <Title level={2}>课题提交</Title>
    </div>
  );
};

export default ProjectSubmit;
