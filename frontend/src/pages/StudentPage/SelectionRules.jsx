import React from "react";
import { Typography } from "antd";

const { Title } = Typography;
const SelectionRules = () => {
  return (
    <div>
      <Title level={2}>选课规则</Title>
    </div>
  );
};

export default SelectionRules;
